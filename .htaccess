# Root .htaccess — redirect all requests to public/
<IfModule mod_rewrite.c>
    RewriteEngine On


    # If the request is not for a real file/directory in public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
